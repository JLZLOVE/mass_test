| 序号 | 操作项                                                       | 具体内容                                                     |
| :--- | :----------------------------------------------------------- | :----------------------------------------------------------- |
| 1    | **扫描 `annotation` 包**                                     | 提取所有自定义注解的定义、用途、属性。                       |
| 2    | **扫描 `config` 包**                                         | 提取配置类及其关键配置。                                     |
| 3    | **扫描 `controller`**                                        | 只需要分析 `SysUserController`、`ysUserRoleController`、`SysRoleController` 的接口、路由、权限注解。 |
| 4    | **扫描 `converter` 包**                                      | 提取转换器类（DTO ↔ Entity）及其映射关系。                   |
| 5    | **处理 `entity` 包**（不扫描，但需修改）                     | 对所有包含 `time` 字段（如 `createTime`、`updateTime`）的实体类，**添加** `@JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss", timezone = "GMT+8")` 注解,只需要更新信息,类已经修改）。 |
| 6    | **跳过 `exception` 包中的** `ConditionConfig.java` 和 `EIException.java`（不处理） | —                                                            |
| 7    | **扫描 `filter` 包**                                         | 提取所有过滤器（如 JWT 认证过滤器、日志过滤器）及其执行顺序。 |
| 8    | **扫描 `security` 包**                                       | 提取安全相关类（如 `SecurityConfig`、`JwtTokenProvider`、`AuthenticationEntryPoint` 等）。 |
| 9    | **读取 `service` 下的** `UserDetailServiceImpl` 和 `SysUserServiceImpl` 及其接口 | 提取用户加载逻辑、业务方法、依赖注入关系。                   |
| 10   | JWT 过期时间                                                 | 过期时间从原值（请告知）延长至 **1 小时**，                  |
| 11   | **更新 `PROJECT_OVERVIEW.md`**                               | 根据以上扫描结果，补充/更新项目结构、核心模块说明、配置变更（特别是 JWT 调整）等。 |