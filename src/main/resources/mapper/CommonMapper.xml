<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="untiy.mapper.CommonMapper">

<select id="requireMapper" resultType="String">
    select distinct ${columnName} from ${tableName}
    <where>
        ${columnName} is not null and ${columnName} !=''
    <if test="conditionColumn !=null and conditionValue !=null">
   and ${conditionColumn}=#{conditionValue}
    </if>
    <if test="level != null">
        and level=#{level}
    </if>
        <if test="parent != null">
         and parent=#{parent}
        </if>
    </where>
</select>
</mapper>
